<%= render "layouts/menuzinho" %> 

<h2><%= @course.name %></h2>
<p><b>Description:</b> <%= @course.description %></p>

<p><%= link_to "Edit Course", edit_course_path(@course) %> | <%= link_to "Delete", course_path(@course), method: :delete, data: { confirm: 'Are you sure?' } %></p>