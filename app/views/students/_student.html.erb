<tr>
	<td class="action"><%= link_to "View", student %>
	<% #if user_signed_in? %>
	 | <%= link_to "Edit", edit_student_path(student) %>
	 <% #end %>
	</td>
	<td><%= student %></td>
	<td><%= student.email %></td>
	<td><%= student.phone %></td>
</tr>