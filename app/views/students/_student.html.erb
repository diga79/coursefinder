<tr>
	<td class="action"><%= link_to "View", student %> 
| <%= link_to "Edit", edit_student_path(student) %> 
| <%= link_to "Delete", student_path(student), method: :delete, data: { confirm: 'Are you sure?' } %> 
| <%= link_to "New Application", new_application_path(student) %> 
	</td>
	<td><%= student %></td>
	<td><%= student.email %></td>
	<td><%= student.phone %></td>
</tr>